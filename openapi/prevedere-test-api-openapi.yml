openapi: 3.2.0
info:
  title: Board Foresight API - V1 Test API
  description: ''
  version: v1
security:
- api key: []
tags:
- name: Test
  description: ''
paths:
  /test:
    get:
      tags:
      - Test
      description: Echos a message string for testing purposes
      parameters:
      - name: Echo
        in: query
        schema:
          type: string
      responses:
        '200':
          description: OK
          content:
            text/plain:
              schema:
                type: string
            application/json:
              schema:
                type: string
            text/json:
              schema:
                type: string
        '429':
          description: If the user has sent too many requests
      summary: Get test
      x-summary-source: derived
      operationId: getTest
      x-operation-id-source: derived
components:
  securitySchemes:
    api_key:
      type: apiKey
      name: ApiKey
      in: query
